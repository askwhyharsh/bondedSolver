// // Deploy factory
// VaultFactory factory = new VaultFactory();

// // Create a new vault for a token pair
// address vault = factory.createVault(token0Address, token1Address);

// // Get vault address for existing pair
// address existingVault = factory.getVault(token0Address, token1Address);
